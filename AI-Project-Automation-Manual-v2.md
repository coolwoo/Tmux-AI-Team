# AI 项目自动化启动器 v2.3 - 操作手册

本手册是快速参考指南。详细使用说明请参阅专题文档：

| 文档 | 内容 |
|------|------|
| [多项目模式](docs/multi-project-mode.md) | Orchestrator 架构，多 Agent 并行开发 |
| [PM 监督模式](docs/pm-oversight-mode.md) | 自动化项目管理，无人值守开发 |
| [最佳实践](docs/best-practices.md) | Git 规范、通信协议、反模式指南 |

---

## 架构概览

### 单项目模式

```
╔═══════════════════════════════════════════════════════════════╗
║                  📦 tmux session: my-project                  ║
╠═══════════════════╦═══════════════════╦═══════════════════════╣
║    Window 0       ║    Window 1       ║      Window 2         ║
║   ┌─────────┐     ║   ┌─────────┐     ║    ┌─────────┐        ║
║   │ 🤖      │     ║   │ 💻      │     ║    │ 🌐      │        ║
║   │ Claude  │     ║   │ Shell   │     ║    │ Server  │        ║
║   │ Agent   │     ║   │         │     ║    │         │        ║
║   └─────────┘     ║   └─────────┘     ║    └─────────┘        ║
╚═══════════════════╩═══════════════════╩═══════════════════════╝
          │
          ├──── ⏰ 自动 Git 提交 (每 30 分钟)
          └──── 📅 自调度检查点
```

### 多项目模式

> 详见 [多项目模式手册](docs/multi-project-mode.md)

```
                    ┌───────────────────────┐
                    │   👤 你 (Orchestrator) │
                    └───────────┬───────────┘
            ┌───────────────────┼───────────────────┐
            ▼                   ▼                   ▼
    ┌───────────────┐   ┌───────────────┐   ┌───────────────┐
    │  📱 frontend  │   │  ⚙️ backend   │   │  📲 mobile    │
    │    Agent      │   │    Agent      │   │    Agent      │
    └───────────────┘   └───────────────┘   └───────────────┘
```

### PM 监督模式

> 详见 [PM 监督模式手册](docs/pm-oversight-mode.md)

```
    ┌─────────────────────────────────────────┐
    │         🎯 PM Agent (项目经理)           │
    │       /pm-oversight 命令启动            │
    └──────────────────┬──────────────────────┘
                       │ 监控、验收、反馈
                       ▼
    ╔══════════════════════════════════════════╗
    ║           📦 Engineer Agent              ║
    ╚══════════════════════════════════════════╝
```

---

## 安装

```bash
# 1. 复制到用户目录
cp bashrc-ai-automation-v2.sh ~/.ai-automation.sh

# 2. 添加 source 语句（自动检查避免重复）
grep -q 'ai-automation.sh' ~/.bashrc || echo '[ -f ~/.ai-automation.sh ] && source ~/.ai-automation.sh' >> ~/.bashrc

# 3. 生效
source ~/.bashrc

# 4. (可选) 安装 at 命令用于自调度
sudo apt install at && sudo systemctl enable --now atd
```

---

## 命令速查

### 环境检查

| 命令 | 说明 |
|------|------|
| `check-deps` | 检查依赖并显示安装建议 |

> source 脚本时会自动进行快速检查，缺少必需依赖时显示提示。

### 基础命令

| 命令 | 说明 |
|------|------|
| `fire <project>` | 快速启动项目 |
| `tsc <target> <msg>` | 发送消息到 Claude |
| `check-agent [session]` | 查看 Agent 状态 |
| `monitor-snapshot [session]` | 生成监控快照 |
| `find-window <name>` | 按名称查找窗口 |
| `stop-project [session]` | 停止项目 |
| `goto <session>` | 切换会话 |

### 多 Agent 命令

| 命令 | 说明 |
|------|------|
| `list-agents` | 列出所有 Agent |
| `send-to-agent <target> <msg>` | 发送消息到指定 Agent |
| `broadcast <msg>` | 广播到所有 Agent |

### 自调度与自动提交

| 命令 | 说明 |
|------|------|
| `schedule-checkin <分钟> <备注>` | 调度下次检查 |
| `start-auto-commit [session] [分钟]` | 启动自动 Git 提交 |
| `stop-auto-commit [session]` | 停止自动提交 |

### 系统健康监控

| 命令 | 说明 |
|------|------|
| `system-health` | 检查所有会话的健康状态 |
| `system-health --save` | 检查并保存日志 |
| `watch-health [分钟]` | 持续监控 (默认每15分钟) |
| `watch-health 10 my-project` | 监控并向指定会话报告状态 |

### 项目规范

| 命令 | 说明 |
|------|------|
| `create-spec <project>` | 创建项目规范文件 |
| `view-spec [project]` | 查看规范 |

### 通信协议

> 详见 [最佳实践](docs/best-practices.md#2-通信规范)

| 命令 | 说明 |
|------|------|
| `send-status` | 状态更新 |
| `send-task` | 任务分配 |
| `send-bug` | Bug 报告 |
| `send-ack` | 确认收到 |
| `send-done` | 完成通知 |
| `send-blocked` | 阻塞通知 |

### 日志系统

| 命令 | 说明 |
|------|------|
| `capture-agent-log <session>` | 捕获对话 |
| `view-agent-logs [session]` | 查看日志 |
| `end-agent <session> <window>` | 结束并保存 |

### Claude Code 斜杠命令

| 命令 | 说明 |
|------|------|
| `/pm-oversight <项目> SPEC: <文件>` | 启动 PM 监督 |
| `/role-developer <任务>` | Developer 角色 |
| `/role-qa <任务>` | QA 角色 |
| `/role-devops <任务>` | DevOps 角色 |
| `/role-reviewer <内容>` | Reviewer 角色 |
| `/deploy-team <项目> [规模]` | 部署团队 |

---

## 快速开始

### 1. 启动单个项目

```bash
# 创建规范 (推荐)
create-spec my-project

# 启动
fire my-project
```

### 2. 项目规范示例

```markdown
# 项目规范: my-project

## 目标
实现用户认证系统

## 交付物
1. 登录/登出接口
2. 用户会话管理

## 约束
- 每 30 分钟提交代码
- 为新功能编写测试
```

### 3. 与 Agent 交互

```bash
# 发送任务
tsc my-project:Claude "请实现登录功能"

# 查看状态
check-agent my-project

# 调度检查
schedule-checkin 30 "检查登录功能"
```

### 4. 多项目协作

```bash
# 启动多个项目（不同终端）
fire frontend
fire backend

# 协调
send-to-agent backend:Claude "请优先完成 /api/auth"
send-to-agent frontend:Claude "等待 backend API"

# 广播
broadcast "准备发布，请提交代码"
```

> 完整指南见 [多项目模式手册](docs/multi-project-mode.md)

### 5. PM 监督模式

```bash
# 终端 1: 启动 Engineer
fire my-project

# 终端 2: 启动 PM
claude
/pm-oversight my-project SPEC: ~/Coding/my-project/project_spec.md
```

> 完整指南见 [PM 监督模式手册](docs/pm-oversight-mode.md)

---

## 配置

```bash
# ~/.bashrc 中设置
export CODING_BASE="$HOME/Projects"  # 项目目录
export CLAUDE_CMD="claude"           # Claude 命令
export DEFAULT_DELAY="1"             # 消息延迟(秒)
```

---

## 常见问题

| 问题 | 解决方案 |
|------|----------|
| 消息发送失败 | 使用 `tsc` 而非 `tmux send-keys` |
| 自调度不工作 | `sudo apt install at && sudo systemctl enable --now atd` |
| Agent 无响应 | `tsc session:Claude "请继续"` 或 `stop-project` 后重启 |

---

## tmux 快捷键

| 快捷键 | 功能 |
|--------|------|
| `Ctrl+b d` | 脱离会话 |
| `Ctrl+b 0/1/2` | 切换窗口 |
| `Ctrl+b w` | 窗口列表 |
| `Ctrl+b s` | 会话列表 |

---

## 版本历史

| 版本 | 更新内容 |
|------|----------|
| v2.3 | 环境自检机制 (check-deps)、依赖守卫、安装建议 |
| v2.2 | Agent 角色模板、通信协议、日志系统 |
| v2.1 | PM 监督模式 |
| v2.0 | 自调度、项目规范、自动提交、多 Agent |
| v1.0 | 初始版本 |
