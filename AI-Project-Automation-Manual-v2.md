# AI 项目自动化启动器 v2.6 - 操作手册

本手册是快速参考指南。详细使用说明请参阅专题文档：

| 文档 | 内容 |
|------|------|
| [快速开始](docs/01-quick-start.md) | 安装配置、5分钟上手 |
| [PM 监督模式](docs/03-pm-oversight-mode.md) | 自动化项目管理，无人值守开发 |
| [Agent 角色](docs/04-agent-roles.md) | 斜杠命令、角色模板 |
| [最佳实践](docs/05-best-practices.md) | Git 规范、通信协议、反模式指南 |

## 核心原则 (v2.6 更新)

- **一项目一PM**：每个 tmux 会话内有一个 PM，只管本项目
- **会话即隔离**：不同项目 = 不同 tmux 会话
- **窗口即槽位**：同一会话内的窗口作为 Agent 槽位
- **窗口名即角色**：从窗口名自动推断角色，零存储

---

## 架构概览

### 项目模式

```
╔═══════════════════════════════════════════════════════════════╗
║                  📦 tmux session: my-project                  ║
╠═══════════════════════════════════════════════════════════════╣
║    Window: Claude                                             ║
║   ┌─────────────────────────────────────────────────────────┐ ║
║   │ 🤖 Claude Agent                                         │ ║
║   │    (其他窗口如 Shell、Server 按需创建)                   │ ║
║   └─────────────────────────────────────────────────────────┘ ║
╚═══════════════════════════════════════════════════════════════╝
          │
          ├──── ⏰ 自动 Git 提交 (每 30 分钟)
          └──── 📅 自调度检查点
```

### PM 监督模式 (项目内)

> 详见 [PM 监督模式手册](docs/03-pm-oversight-mode.md)

```
┌─────────────────────────────────────────────────────┐
│            📦 tmux session: my-project               │
├─────────┬───────────┬───────────┬───────────────────┤
│ Claude  │   dev-1   │   dev-2   │       qa          │
│  (PM)   │(Developer)│(Developer)│      (QA)         │
├─────────┴───────────┴───────────┴───────────────────┤
│ • PM 只管本会话内的槽位                               │
│ • 角色从窗口名自动推断                               │
│ • 项目间通过不同会话隔离                             │
└─────────────────────────────────────────────────────┘
```

### 窗口命名规范

| 窗口名模式 | 推断角色 |
|------------|----------|
| `dev`, `dev-1`, `dev-*` | Developer |
| `qa`, `qa-1`, `qa-*` | QA |
| `devops`, `devops-*` | DevOps |
| `reviewer`, `reviewer-*` | Reviewer |
| `Claude`, `PM` | PM |
| `Shell`, `Server` | Shell |
| 其他 | Unknown |

---

## 安装

```bash
# 1. 复制到用户目录
cp bashrc-ai-automation-v2.sh ~/.ai-automation.sh

# 2. 添加 source 语句（自动检查避免重复）
grep -q 'ai-automation.sh' ~/.bashrc || echo '[ -f ~/.ai-automation.sh ] && source ~/.ai-automation.sh' >> ~/.bashrc

# 3. 生效
source ~/.bashrc

# 4. (可选) 安装 at 命令用于自调度
sudo apt install at && sudo systemctl enable --now atd

# 5. Agent 上下文（自动配置）
# fire 启动时自动复制 .claude/TMUX_AI.md 到目标项目
# 详见 docs/01-quick-start.md 第 2.5 节
```

---

## 命令速查

### 环境检查

| 命令 | 说明 |
|------|------|
| `check-deps` | 检查依赖并显示安装建议 |

> source 脚本时会自动进行快速检查，缺少必需依赖时显示提示。

### 基础命令

| 命令 | 说明 |
|------|------|
| `fire <project>` | 快速启动项目（显示项目类型） |
| `tsc <target> <msg>` | 发送消息到 Claude |
| `check-agent [session]` | 查看 Agent 状态 |
| `monitor-agent [session]` | 实时监控 Agent |
| `monitor-snapshot [session]` | 生成监控快照 |
| `find-window <name>` | 按名称查找窗口 |
| `stop-project [session]` | 停止项目 |
| `goto <session>` | 切换会话 |

### 消息发送

| 命令 | 说明 |
|------|------|
| `tsc <target> <msg>` | 发送消息（自动添加发送方窗口前缀） |
| `tsc -q <target> <msg>` | 静默模式发送消息 |
| `tsc -r <target> <msg>` | 原始模式（不加前缀） |
| `tsc -q -r <target> <msg>` | 静默 + 原始模式 |
| `get-role [window]` | 从窗口名推断角色 |

**消息溯源**: `tsc` 默认会在消息前添加 `[窗口名]` 前缀，方便 PM 追踪消息来源：

```bash
# 在 dev-1 窗口执行:
tsc my-project:Claude "任务完成"
# PM 收到: [dev-1] 任务完成

# 内部命令使用 -r 避免双重前缀:
tsc -r my-project:Claude "[PM 广播] 准备发布"
```

### 自调度与自动提交

| 命令 | 说明 |
|------|------|
| `schedule-checkin <分钟> <备注>` | 调度下次检查 |
| `read-next-note [target]` | 读取下次检查备注 |
| `start-auto-commit [session] [分钟]` | 启动自动 Git 提交 |
| `stop-auto-commit [session]` | 停止自动提交 |

### 系统健康监控

| 命令 | 说明 |
|------|------|
| `system-health` | 检查所有会话的健康状态 |
| `system-health --save` | 检查并保存日志 |
| `watch-health [分钟]` | 持续监控 (默认每15分钟) |
| `watch-health 10 my-project` | 监控并向指定会话报告状态 |

### 通信协议

> 详见 [最佳实践](docs/05-best-practices.md#2-通信规范)

| 命令 | 说明 |
|------|------|
| `send-status` | 状态更新 |
| `send-task` | 任务分配 |
| `send-bug` | Bug 报告 |
| `send-ack` | 确认收到 |
| `send-done` | 完成通知 |
| `send-blocked` | 阻塞通知 |

### 日志系统

日志保存在 `~/.agent-logs/` 目录 (可通过 `AGENT_LOG_DIR` 环境变量修改)。

| 命令 | 说明 |
|------|------|
| `init-agent-logs` | 初始化日志目录 |
| `capture-agent-log <session>` | 捕获对话 |
| `view-agent-logs [session]` | 查看今日日志 |
| `clean-agent-logs [days]` | 清理旧日志 (默认7天) |
| `end-agent <session> <window>` | 结束并保存 |

日志文件类型：
- `health_*.log` - 系统健康检查报告
- `<session>_<date>.log` - 会话日志
- `<session>_<window>_*.log` - 窗口快照

### PM 槽位管理 (v3.5)

| 命令 | 说明 |
|------|------|
| `pm-init-slots` | 初始化 3 个槽位 (dev-1, dev-2, qa) |
| `pm-assign <slot> <role> <task>` | 分配任务到槽位 |
| `pm-status` | 查看槽位状态面板（含角色列） |
| `pm-check <slot>` | 智能检测槽位状态 (解析 [STATUS:*]) |
| `pm-mark <slot> <status>` | 手动标记状态 |
| `pm-broadcast <msg>` | 广播消息到工作中的槽位 |
| `pm-history` | 查看 PM 操作历史 |
| `get-role [window]` | 从窗口名推断角色 |

**pm-status 输出示例**:

```
╔═══════════════════════════════════════════════════════════════════════════╗
║                      PM 状态面板  14:30:00                                 ║
╠══════════╦════╦═══════════╦══════════╦══════════════════════════════╣
║ 槽位     ║类型║ 角色      ║ 状态     ║ 任务                         ║
╠══════════╬════╬═══════════╬══════════╬══════════════════════════════╣
║ dev-1    ║ 🤖 ║ Developer ║ 🟢 working ║ 实现用户认证               ║
║ dev-2    ║ 🤖 ║ Developer ║ ⚪ idle    ║ -                          ║
║ qa       ║ 🤖 ║ QA        ║ 🔵 ready   ║ 等待测试                   ║
╚══════════╩════╩═══════════╩══════════╩══════════════════════════════════╝
```

### Claude Code 斜杠命令

> 斜杠命令位于 `.claude/commands/tmuxAI/` 目录

**PM 槽位命令 (v3.5)**:

| 命令 | 说明 |
|------|------|
| `/tmuxAI:pm-init` | 初始化槽位 |
| `/tmuxAI:pm-assign <slot> <role> <task>` | 分配任务 |
| `/tmuxAI:pm-status` | 状态面板 |
| `/tmuxAI:pm-check <slot>` | 智能检测 |
| `/tmuxAI:pm-mark <slot> <status>` | 标记状态 |
| `/tmuxAI:pm-broadcast <msg>` | 广播消息 |
| `/tmuxAI:pm-history` | 操作历史 |

**原有命令**:

| 命令 | 说明 |
|------|------|
| `/tmuxAI:pm-oversight <项目> [任务描述]` | 启动 PM 监督 |
| `/tmuxAI:role-developer <任务>` | Developer 角色 |
| `/tmuxAI:role-qa <任务>` | QA 角色 |
| `/tmuxAI:role-devops <任务>` | DevOps 角色 |
| `/tmuxAI:role-reviewer <内容>` | Reviewer 角色 |
| `/tmuxAI:deploy-team <项目> [规模]` | 部署团队 |

---

## 快速开始

### 1. 启动单个项目

```bash
# 启动项目
fire my-project
```

### 2. 与 Agent 交互

```bash
# 发送任务
tsc my-project:Claude "请实现登录功能"

# 查看状态
check-agent my-project

# 调度检查
schedule-checkin 30 "检查登录功能"
```

### 3. PM 监督模式 (项目内)

```bash
# 启动项目（PM 在 Claude 窗口）
fire my-project

# PM 初始化槽位
pm-init-slots

# PM 分配任务到 dev-1 槽位
pm-assign dev-1 developer "实现用户认证系统"

# PM 查看状态面板
pm-status

# Engineer 完成后通过状态标记通知 PM
# [STATUS:DONE] 用户认证已完成
```

> 完整指南见 [PM 监督模式手册](docs/03-pm-oversight-mode.md)

---

## 配置

```bash
# ~/.bashrc 中设置
export CODING_BASE="$HOME/Projects"  # 项目目录
export CLAUDE_CMD="claude"           # Claude 命令
export DEFAULT_DELAY="1"             # 消息延迟(秒)
```

---

## 常见问题

| 问题 | 解决方案 |
|------|----------|
| 消息发送失败 | 使用 `tsc` 而非 `tmux send-keys` |
| 自调度不工作 | `sudo apt install at && sudo systemctl enable --now atd` |
| Agent 无响应 | `tsc session:Claude "请继续"` 或 `stop-project` 后重启 |

---

## tmux 快捷键

| 快捷键 | 功能 |
|--------|------|
| `<prefix> d` | 脱离会话 |
| `<prefix> 0/1/2` | 切换窗口 |
| `<prefix> w` | 窗口列表 |
| `<prefix> s` | 会话列表 |

> **tmux 前缀键**: `<prefix>` 默认是 `Ctrl+b`，可在 `~/.tmux.conf` 中自定义（常见替代：`Ctrl+a`）。查看当前前缀：`tmux show-options -g prefix`

---

## 版本历史

| 版本 | 更新内容 |
|------|----------|
| v2.6 | 项目隔离 PM：`get-role()` 角色推断、`tsc -r` 原始模式、`pm-status` 角色列、移除多项目模式 |
| v2.5 | PM 槽位管理 v3.4 (pm-init/assign/status/check/mark/broadcast/history)、状态标记协议 |
| v2.4 | Agent 上下文配置、项目类型检测、等待 Claude 启动、合并脚本版本 |
| v2.3 | 环境自检机制 (check-deps)、依赖守卫、安装建议 |
| v2.2 | Agent 角色模板、通信协议、日志系统 |
| v2.1 | PM 监督模式 |
| v2.0 | 自调度、项目规范、自动提交 |
| v1.0 | 初始版本 |
