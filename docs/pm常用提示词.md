# PM 常用提示词

> 📅 Last updated: 2026-01-17

本文档提供自然语言提示词，用于向 PM Agent 发出指令。直接复制或根据需求修改参数。

---

## 槽位管理

### 初始化与添加

| 提示词 | 对应命令 | 说明 |
|--------|----------|------|
| 初始化槽位 | `/tmuxAI:pm-init` | 创建 dev-1 Claude 槽位 |
| 添加 dev-2 槽位 | `pm-add-slot dev-2 --claude` | Claude 槽位 |
| 添加 qa 槽位 | `pm-add-slot qa --claude` | Claude 槽位 |
| 添加 Shell 窗口 | `pm-add-slot server --shell` | 命令执行窗口 |
| 列出所有槽位 | `pm-list-slots` | 显示槽位列表 |
| 删除 qa 槽位 | `pm-remove-slot qa` | 删除槽位 |

**槽位类型**:
- `--claude`: AI Agent 槽位，自动启动 Claude (🤖)
- `--shell`: 命令执行窗口 (🖥️)

### 查看状态

| 提示词 | 对应命令 | 说明 |
|--------|----------|------|
| 查看状态面板 | `/tmuxAI:pm-status` | 显示所有槽位状态 |
| 检查 dev-1 状态 | `/tmuxAI:pm-check dev-1` | 智能检测（解析 [STATUS:*]） |
| 查看操作历史 | `/tmuxAI:pm-history` | PM 操作日志 |

### 分配任务

| 提示词 | 对应命令 |
|--------|----------|
| 给 dev-1 分配开发任务 | `/tmuxAI:pm-assign dev-1 role-developer "实现用户登录"` |
| 让 qa 测试登录功能 | `/tmuxAI:pm-assign qa role-qa "测试登录功能"` |
| 让 dev-2 做代码审查 | `/tmuxAI:pm-assign dev-2 role-reviewer "审查 PR #123"` |

### 标记状态

| 提示词 | 对应命令 |
|--------|----------|
| 把 dev-1 标记为空闲 | `/tmuxAI:pm-mark dev-1 idle` |
| 标记 dev-1 已完成 | `/tmuxAI:pm-mark dev-1 done` |

### 广播消息

| 提示词 | 对应命令 |
|--------|----------|
| 通知所有 Agent 汇报进度 | `/tmuxAI:pm-broadcast "请汇报进度"` |
| 广播暂停工作 | `/tmuxAI:pm-broadcast "暂停当前工作"` |

---

## 监控与通信

### 查看输出

| 提示词 | 对应命令 |
|--------|----------|
| 看看 dev-1 在做什么 | `pm-get-output dev-1 50` |
| 检查 dev-1 是否有错误 | `pm-get-output dev-1 | grep -iE "error\|failed"` |

### 等待结果

| 提示词 | 对应命令 |
|--------|----------|
| 等待 dev-1 完成 | `pm-wait-result dev-1 120` |
| 发消息给 dev-1 并等结果 | `pm-send-and-wait dev-1 "请总结进度" 60` |

### 定时检查

| 提示词 | 对应命令 |
|--------|----------|
| 15 分钟后提醒检查 | `schedule-checkin 15 "检查进度"` |
| 30 分钟后检查所有槽位 | `schedule-checkin 30 "检查 dev-1 dev-2 qa"` |

### 直接发送消息

| 提示词 | 对应命令 |
|--------|----------|
| 告诉 dev-1 优先处理 bug | `tsc dev-1 "优先处理登录 bug"` |

---

## 状态标记协议

Agent 完成任务时应输出状态标记，PM 使用 `/tmuxAI:pm-check` 自动检测：

| 状态 | 标记格式 |
|------|----------|
| 完成 | `[STATUS:DONE] 任务完成说明` |
| 错误 | `[STATUS:ERROR] 错误说明` |
| 阻塞 | `[STATUS:BLOCKED] 阻塞原因` |
| 进度 | `[STATUS:PROGRESS] 进度说明` |

---

## 典型工作流

### 多 Agent 协作

```
1. 初始化槽位                           # /tmuxAI:pm-init
2. 添加 dev-2 槽位                      # pm-add-slot dev-2 --claude
3. 添加 qa 槽位                         # pm-add-slot qa --claude
4. 给 dev-1 分配任务：实现后端 API
5. 给 dev-2 分配任务：实现前端界面
6. 15 分钟后提醒检查进度
7. 查看状态面板                         # /tmuxAI:pm-status
8. 收到 [STATUS:DONE] → 验收
9. 让 qa 测试功能
10. 删除 qa 槽位（完成后）
```

### 问题排查

```
1. 查看状态面板                         # 检查是否有 working? 过时状态
2. 检查 dev-1 状态                      # /tmuxAI:pm-check dev-1
3. 看看 dev-1 在做什么                  # pm-get-output dev-1 50
4. 把 dev-1 标记为空闲（如需重置）      # /tmuxAI:pm-mark dev-1 idle
```

---

## 注意事项

| 问题 | 说明 |
|------|------|
| 状态过时 | `working?` 表示可能过时，用 `pm-check` 或 `pm-mark` 更新 |
| Shell 槽位限制 | 不能用 `pm-assign` 分配任务 |
| 槽位删除保护 | 工作中的槽位需 `--force` 才能删除 |
| 定时检查依赖 | 需要 `at` 服务运行 (`systemctl status atd`) |

---

## 相关文档

- [PM 监督模式](03-pm-oversight-mode.md)
- [Agent 角色指南](04-agent-roles.md)
- [最佳实践](05-best-practices.md)
