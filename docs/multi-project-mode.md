# 多项目模式 (Orchestrator 架构) 使用手册

## 概述

多项目模式允许你同时管理多个 AI Agent，每个 Agent 负责一个独立的项目。你作为 Orchestrator（协调者）监控和协调所有 Agent 的工作。

## 架构图

```
                ╔═══════════════════════════════════════════╗
                ║         👤 你 (Orchestrator)              ║
                ║  ┌─────────────────────────────────────┐  ║
                ║  │  📊 监控所有 Agent 状态             │  ║
                ║  │  🔗 协调跨项目依赖                  │  ║
                ║  │  📋 分配任务和优先级               │  ║
                ║  │  🔧 处理跨项目问题                  │  ║
                ║  └─────────────────────────────────────┘  ║
                ╚══════════════════╦════════════════════════╝
                                   │
          ┌────────────────────────┼────────────────────────┐
          │                        │                        │
          ▼                        ▼                        ▼
┌─────────────────────┐  ┌─────────────────────┐  ┌─────────────────────┐
│  📱 frontend Agent  │  │  ⚙️ backend Agent   │  │  📲 mobile Agent    │
├─────────────────────┤  ├─────────────────────┤  ├─────────────────────┤
│ ╭─────────────────╮ │  │ ╭─────────────────╮ │  │ ╭─────────────────╮ │
│ │ 🤖 Window 0     │ │  │ │ 🤖 Window 0     │ │  │ │ 🤖 Window 0     │ │
│ │    Claude       │ │  │ │    Claude       │ │  │ │    Claude       │ │
│ ├─────────────────┤ │  │ ├─────────────────┤ │  │ ├─────────────────┤ │
│ │ 💻 Window 1     │ │  │ │ 💻 Window 1     │ │  │ │ 💻 Window 1     │ │
│ │    Shell        │ │  │ │    Shell        │ │  │ │    Shell        │ │
│ ├─────────────────┤ │  │ ├─────────────────┤ │  │ ├─────────────────┤ │
│ │ 🌐 Window 2     │ │  │ │ 🌐 Window 2     │ │  │ │ 🌐 Window 2     │ │
│ │    Server       │ │  │ │    Server       │ │  │ │    Server       │ │
│ ╰─────────────────╯ │  │ ╰─────────────────╯ │  │ ╰─────────────────╯ │
└─────────────────────┘  └─────────────────────┘  └─────────────────────┘
```

## 适用场景

| 场景 | 说明 |
|------|------|
| 前后端分离项目 | frontend + backend 同时开发 |
| 微服务架构 | 多个服务并行开发 |
| 多平台应用 | Web + Mobile + Desktop |
| 大型重构 | 多个模块同时修改 |

---

## 快速开始

### 1. 准备工作

确保已安装 bash 函数：

```bash
# 安装
cp bashrc-ai-automation-v2.sh ~/.ai-automation.sh
echo '[ -f ~/.ai-automation.sh ] && source ~/.ai-automation.sh' >> ~/.bashrc
source ~/.bashrc
```

### 2. 启动多个项目

在**不同的终端**中分别启动：

```bash
# 终端 1: 启动 frontend
fire frontend

# 终端 2: 启动 backend
fire backend

# 终端 3: 启动 mobile
fire mobile
```

或者使用 `--no-attach` 选项在后台启动：

```bash
# 在同一终端启动多个项目（不附加）
project-start frontend --no-attach
project-start backend --no-attach
project-start mobile --no-attach

# 然后选择要查看的项目
goto frontend
```

### 3. 查看所有 Agent

```bash
list-agents
```

输出示例：
```
活跃的 Agent 会话:
  frontend (3 窗口)
  backend (3 窗口)
  mobile (3 窗口)
```

---

## 核心操作

### 监控所有 Agent

```bash
# 生成所有会话的监控快照
monitor-snapshot

# 查看指定 Agent 状态
check-agent frontend
check-agent backend
```

### 向单个 Agent 发送消息

```bash
# 向 frontend Agent 发送任务
tsc frontend:Claude "请实现用户登录页面"

# 向 backend Agent 发送任务
tsc backend:Claude "请实现 /api/auth/login 接口"
```

### 跨项目协调

```bash
# 通知 frontend 等待 backend
send-to-agent frontend:Claude "请等待 backend 完成 API，预计 30 分钟"

# 通知 backend 优先完成某接口
send-to-agent backend:Claude "frontend 需要 /api/users 接口，请优先处理"
```

### 广播消息

```bash
# 向所有 Agent 广播
broadcast "准备发布 v1.0，请完成当前任务并提交代码"

# 暂停所有工作
broadcast "暂停工作，等待架构评审"

# 恢复工作
broadcast "继续工作"
```

### 查找窗口

```bash
# 查找所有 Claude 窗口
find-window Claude

# 查找所有 Server 窗口
find-window Server
```

### 切换会话

```bash
# 切换到 frontend
goto frontend

# 切换到 backend
goto backend
```

---

## 工作流程

### 标准工作流

```
    ╔═══════════════════════════════════════════════════╗
    ║              🚀 阶段 1: 启动                       ║
    ╚═══════════════════════════════════════════════════╝
                            │
        ┌───────────────────┴───────────────────┐
        │                                       │
        ▼                                       ▼
   ┌─────────────┐                       ┌─────────────┐
   │ 📝 创建规范  │                       │ 🔥 启动项目  │
   │ create-spec │                       │    fire     │
   │  frontend   │                       │  frontend   │
   │  backend    │                       │  backend    │
   └─────────────┘                       └─────────────┘
                            │
                            ▼
    ╔═══════════════════════════════════════════════════╗
    ║              📊 阶段 2: 监控                       ║
    ╚═══════════════════════════════════════════════════╝
                            │
                            ▼
              ┌─────────────────────────┐
              │  👀 定期检查状态         │
              │  ┌───────────────────┐  │
              │  │ monitor-snapshot  │  │
              │  │ check-agent xxx   │  │
              │  └───────────────────┘  │
              └─────────────────────────┘
                            │
                            ▼
    ╔═══════════════════════════════════════════════════╗
    ║              🔗 阶段 3: 协调                       ║
    ╚═══════════════════════════════════════════════════╝
                            │
                            ▼
              ┌─────────────────────────┐
              │  🔧 处理跨项目依赖       │
              │  • 识别阻塞点           │
              │  • 调整优先级           │
              │  • 传递接口信息         │
              └─────────────────────────┘
                            │
                            ▼
    ╔═══════════════════════════════════════════════════╗
    ║              ✅ 阶段 4: 完成                       ║
    ╚═══════════════════════════════════════════════════╝
                            │
                            ▼
              ┌─────────────────────────┐
              │  🎯 验收和合并           │
              │  • 检查完成状态         │
              │  • 集成测试             │
              │  • 合并代码             │
              └─────────────────────────┘
```

### 处理跨项目依赖示例

**场景**: Frontend 需要 Backend 提供的 API

```bash
# 1. 通知 Backend 优先级
send-to-agent backend:Claude "Frontend 需要以下 API，请优先实现：
- POST /api/auth/login
- GET /api/users/me
- PUT /api/users/me"

# 2. 通知 Frontend 等待
send-to-agent frontend:Claude "请先实现不依赖 API 的部分（UI 布局、表单验证）。
Backend 正在实现 API，预计 1 小时后可用。"

# 3. Backend 完成后通知 Frontend
send-to-agent frontend:Claude "API 已就绪：
- POST /api/auth/login - 登录接口
- GET /api/users/me - 获取当前用户
- PUT /api/users/me - 更新用户信息
请开始集成。"
```

---

## 监控策略

### 定时检查脚本

创建一个定时检查脚本：

```bash
#!/bin/bash
# check-all-agents.sh

echo "========================================"
echo "多项目状态检查 - $(date)"
echo "========================================"

# 生成快照
monitor-snapshot

# 检查是否有错误
echo ""
echo "=== 错误汇总 ==="
for session in $(tmux list-sessions -F "#{session_name}" 2>/dev/null); do
    errors=$(tmux capture-pane -t "$session:Server" -p 2>/dev/null | grep -iE "(error|failed)" | tail -3)
    if [ -n "$errors" ]; then
        echo "[$session] 检测到错误:"
        echo "$errors"
        echo ""
    fi
done
```

### 使用 watch 实时监控

```bash
# 每 30 秒刷新一次状态
watch -n 30 'monitor-snapshot'
```

---

## 常见场景

### 场景 1: 前后端联调

```bash
# 1. 启动项目
fire frontend
fire backend

# 2. Backend 先开发 API
send-to-agent backend:Claude "请先实现用户认证 API"

# 3. Frontend 开发 UI
send-to-agent frontend:Claude "请先实现登录页面 UI，使用 mock 数据"

# 4. 联调
send-to-agent frontend:Claude "Backend API 已就绪，请替换 mock 数据进行联调"
```

### 场景 2: 微服务开发

```bash
# 启动多个服务
fire user-service
fire order-service
fire payment-service

# 协调服务间依赖
send-to-agent order-service:Claude "user-service 已完成用户验证接口，请集成"
send-to-agent payment-service:Claude "order-service 订单创建接口已就绪，请实现支付流程"
```

### 场景 3: 紧急修复

```bash
# 广播紧急通知
broadcast "紧急：发现生产环境 bug，请暂停当前工作"

# 指定 Agent 处理
send-to-agent backend:Claude "请紧急修复 /api/orders 接口的空指针问题"

# 其他 Agent 继续
send-to-agent frontend:Claude "Backend 在处理紧急 bug，请继续其他工作"
```

---

## 最佳实践

### 1. 项目规范先行

```bash
# 启动前先创建规范
create-spec frontend
create-spec backend

# 编辑规范，明确职责边界
vim ~/Coding/frontend/project_spec.md
vim ~/Coding/backend/project_spec.md
```

### 2. 明确接口契约

在开始前，定义好跨项目接口：

```bash
# 创建共享的 API 规范
cat > ~/Coding/shared/api-spec.md << 'EOF'
# API 规范

## 用户认证
- POST /api/auth/login
- POST /api/auth/logout
- GET /api/auth/me

## 响应格式
{
  "success": boolean,
  "data": object,
  "error": string
}
EOF

# 通知各 Agent
broadcast "请阅读 ~/Coding/shared/api-spec.md 了解接口规范"
```

### 3. 定期同步状态

```bash
# 每小时检查一次
while true; do
    monitor-snapshot
    sleep 3600
done
```

### 4. 及时处理阻塞

```bash
# 发现 frontend 被阻塞
check-agent frontend
# 输出显示："等待 backend API..."

# 立即协调
send-to-agent backend:Claude "frontend 被阻塞，请优先完成 /api/users 接口"
```

---

## 命令速查表

| 命令 | 说明 |
|------|------|
| `fire <project>` | 启动项目 |
| `list-agents` | 列出所有 Agent |
| `monitor-snapshot` | 生成监控快照 |
| `check-agent <session>` | 检查单个 Agent |
| `tsc <target> <msg>` | 发送消息 |
| `send-to-agent <target> <msg>` | 向 Agent 发送消息 |
| `broadcast <msg>` | 广播到所有 Agent |
| `find-window <name>` | 查找窗口 |
| `goto <session>` | 切换会话 |
| `stop-project <session>` | 停止项目 |

---

## 故障排除

### 问题: Agent 无响应

```bash
# 检查状态
check-agent <session>

# 尝试发送唤醒消息
tsc <session>:Claude "请继续工作"

# 如果仍无响应，重启
stop-project <session>
fire <project>
```

### 问题: 消息发送失败

```bash
# 确认会话存在
tmux has-session -t <session> && echo "存在" || echo "不存在"

# 确认窗口名称正确
tmux list-windows -t <session>
```

### 问题: 跨项目通信混乱

```bash
# 使用广播统一状态
broadcast "请暂停工作，等待协调指令"

# 逐个发送明确指令
send-to-agent frontend:Claude "你的任务: ..."
send-to-agent backend:Claude "你的任务: ..."
```
