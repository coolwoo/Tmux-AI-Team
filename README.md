# Tmux-AI-Team

AI 项目自动化工具包 - 将 tmux 与 Claude Code 集成，实现自主开发工作流。

## 设计理念

本项目的核心思想是**利用 tmux 的天然特性实现 AI Agent 的协作与管理**，而非重新发明轮子。

### 四大核心原则

| 原则 | 说明 | 价值 |
|------|------|------|
| **一项目一PM** | 每个 tmux 会话内有一个 PM Agent | PM 只管本项目，职责清晰 |
| **会话即隔离** | 不同项目 = 不同 tmux 会话 | 天然隔离，互不干扰 |
| **窗口即槽位** | 同一会话内的窗口作为 Agent 工作槽位 | 复用 tmux 窗口管理 |
| **窗口名即角色** | 从窗口名自动推断角色 (dev-1→Developer) | **零存储、零持久化** |

### 为什么这样设计？

```
传统方案:                              本项目方案:
┌────────────────────────┐            ┌────────────────────────┐
│  需要数据库存储状态      │            │  tmux 环境变量存储状态   │
│  需要进程管理器         │            │  tmux 管理会话生命周期   │
│  需要消息队列           │            │  tmux send-keys 传递消息 │
│  需要服务发现           │            │  窗口名就是服务标识      │
│  复杂的故障恢复         │            │  tmux 崩溃重启即恢复     │
└────────────────────────┘            └────────────────────────┘
```

**核心洞察**：tmux 本身就是一个成熟的会话管理器，具备持久化、窗口管理、消息传递等能力。我们只需在其上构建薄薄的一层协调逻辑。

## 系统架构

### 四层架构

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         Tmux-AI 系统架构                                 │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  ┌────────────────────────────────────────────────────────────────────┐ │
│  │                        应用层 (斜杠命令)                            │ │
│  │  /tmuxAI:start:pm-oversight   /tmuxAI:pm:assign   /tmuxAI:roles:*  │ │
│  │  用户通过斜杠命令与系统交互，激活角色、分配任务                        │ │
│  └────────────────────────────────────────────────────────────────────┘ │
│                                    │                                     │
│                                    ▼                                     │
│  ┌────────────────────────────────────────────────────────────────────┐ │
│  │                        自动化层                                     │ │
│  │  schedule-checkin │ _pm_stop_hook │ _pm_prompt_hook │ auto-commit  │ │
│  │     (拉取式)      │   (推送式)     │   (推送式)       │   (定时式)   │ │
│  │  Agent 自我唤醒    │ 任务完成通知    │ 人类介入检测      │ 定时Git提交  │ │
│  └────────────────────────────────────────────────────────────────────┘ │
│                                    │                                     │
│                                    ▼                                     │
│  ┌────────────────────────────────────────────────────────────────────┐ │
│  │                        通信层                                       │ │
│  │            tsc (消息发送)  │  send-status (状态协议)  │  broadcast   │ │
│  │            自动添加来源     │  结构化状态汇报          │  批量通知    │ │
│  └────────────────────────────────────────────────────────────────────┘ │
│                                    │                                     │
│                                    ▼                                     │
│  ┌────────────────────────────────────────────────────────────────────┐ │
│  │                        基础设施层                                   │ │
│  │              tmux (会话管理)  │  at (定时任务)  │  git (版本控制)    │ │
│  │              会话持久化        │  系统级调度     │  进度保存          │ │
│  └────────────────────────────────────────────────────────────────────┘ │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

### PM 监督模式架构

```
┌─────────────────────────────────────────────────────────────────┐
│                   tmux session: my-project                       │
├──────────┬──────────┬──────────┬──────────┬─────────────────────┤
│  Claude  │  dev-1   │  dev-2   │    qa    │                     │
│   (PM)   │(Developer)│(Developer)│   (QA)   │   窗口名 = 角色     │
├──────────┴──────────┴──────────┴──────────┤                     │
│                                            │                     │
│   PM 分配任务 ──────────────▶ Engineer     │   pm-assign         │
│                                            │                     │
│   Engineer 完成 ──[STATUS:DONE]──▶ Hook   │   状态标记           │
│                                            │                     │
│   Hook 通知 ──────────────────▶ PM        │   _pm_stop_hook     │
│                                            │                     │
└────────────────────────────────────────────┴─────────────────────┘
```

### Hook 推送机制

**解决的问题**：PM 如何知道 Engineer Agent 完成了任务？

```
传统轮询方案:                          Hook 推送方案:
┌─────────────────────┐               ┌─────────────────────┐
│ PM 每隔 N 分钟检查   │               │ Engineer 输出        │
│ 所有槽位状态         │               │ [STATUS:DONE]       │
│ (浪费资源、延迟高)   │               │        ↓            │
└─────────────────────┘               │ Claude Stop 事件     │
                                      │        ↓            │
                                      │ _pm_stop_hook 触发   │
                                      │        ↓            │
                                      │ 自动通知 PM          │
                                      │ (实时、零轮询)        │
                                      └─────────────────────┘
```

**状态标记协议**：

| 标记 | 含义 | PM 收到的通知 |
|------|------|--------------|
| `[STATUS:DONE]` | 任务完成 | `[Hook] dev-1 → done: 任务描述 (耗时: 15分钟)` |
| `[STATUS:ERROR]` | 遇到错误 | `[Hook] dev-1 → error: 错误描述` |
| `[STATUS:BLOCKED]` | 任务阻塞 | `[Hook] dev-1 → blocked: 阻塞原因` |

## 功能特性

- **自主开发**: 在 tmux 会话中启动 Claude Code Agent 进行项目开发
- **自调度**: Agent 使用 `at` 命令安排自己的下次检查时间，实现无人值守
- **PM 监督模式**: 项目内 PM 自动监督开发 Agent，Hook 推送状态变更
- **团队协作**: 支持 Developer、QA、DevOps、Reviewer 等角色
- **多设备同步**: 通过 SSH + tmux 在任意设备上监控和操作 Agent
- **环境自检**: 自动检测依赖并提供系统对应的安装建议

## 系统要求

- Linux / macOS / WSL
- tmux
- [Claude Code](https://claude.ai/code) CLI
- `at` 命令 (用于自调度功能)

## 安装

### 1. 克隆仓库

```bash
git clone https://github.com/coolwoo/Tmux-AI-Team.git
cd Tmux-AI-Team
```

### 2. 安装 Bash 函数

```bash
# 复制到用户目录
cp bashrc-ai-automation-v2.sh ~/.ai-automation.sh

# 在 ~/.bashrc 中添加 source 语句
grep -q 'ai-automation.sh' ~/.bashrc || echo '[ -f ~/.ai-automation.sh ] && source ~/.ai-automation.sh' >> ~/.bashrc

# 重新加载
source ~/.bashrc
```

**或使用符号链接（便于更新）：**
```bash
ln -sf ~/Coding/Tmux-AI-Team/bashrc-ai-automation-v2.sh ~/.ai-automation.sh
```

### 3. 安装 at 命令 (自调度功能)

```bash
# Ubuntu/Debian
sudo apt install at
sudo systemctl enable --now atd

# macOS - at 命令已预装
```

### 4. 检查环境

```bash
check-deps
```

## 快速开始

### 启动项目

```bash
# 快速启动（自动创建 tmux 会话并启动 Claude）
fire my-project
```

### PM 监督模式 (推荐)

```bash
# 在 Claude 窗口中执行
/tmuxAI:start:pm-oversight

# PM 初始化槽位
/tmuxAI:pm:1-init

# PM 分配任务
/tmuxAI:pm:2-assign dev-1 developer "实现用户登录API"

# 查看状态面板
/tmuxAI:pm:3-status
```

### 自调度示例

Agent 可以安排自己的下次唤醒时间：

```bash
# Agent 执行：30 分钟后检查测试结果
schedule-checkin 30 "检查集成测试结果"

# Agent 停止工作，30 分钟后被自动唤醒继续
```

## 核心命令

### 基础命令

| 命令 | 说明 |
|------|------|
| `fire <project>` | 快速启动项目 |
| `tsc <target> <msg>` | 发送消息（自动添加来源前缀） |
| `check-deps` | 检查依赖并显示安装建议 |
| `get-role [window]` | 从窗口名推断角色 |
| `schedule-checkin <分钟> <备注>` | 调度下次检查 |

### PM 槽位管理

| 命令 | 说明 |
|------|------|
| `pm-init-slots` | 初始化槽位 (dev-1, dev-2, qa) |
| `pm-assign <slot> <role> <task>` | 分配任务到槽位 |
| `pm-status` | 查看槽位状态面板 |
| `pm-check <slot>` | 智能检测槽位状态 |
| `pm-broadcast <msg>` | 广播消息到工作中的槽位 |

## 多设备同步

基于 tmux 的会话持久化特性，可从任意设备连接到同一开发环境：

```
┌─────────────────────────────────────────────────────────────────┐
│                    开发服务器 (Linux/WSL)                        │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │                   tmux 会话持久化                         │    │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐      │    │
│  │  │ my-project  │  │  backend    │  │  frontend   │      │    │
│  │  │ PM + Agents │  │ PM + Agents │  │ PM + Agents │      │    │
│  │  └─────────────┘  └─────────────┘  └─────────────┘      │    │
│  └─────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
          ▲                    ▲                    ▲
          │ SSH                │ SSH                │ SSH
    ┌─────┴─────┐        ┌─────┴─────┐        ┌─────┴─────┐
    │    📱     │        │    💻     │        │    📱     │
    │   手机    │        │    PC     │        │   平板    │
    │  (监控)   │        │  (主操作) │        │  (监控)   │
    └───────────┘        └───────────┘        └───────────┘
```

```bash
# 从任意设备 SSH 连接后
goto my-project
# 或
tmux attach -t my-project
```

## 配置

在 `~/.bashrc` 中设置环境变量：

```bash
export CODING_BASE=~/Coding              # 项目根目录
export CLAUDE_CMD=claude                 # Claude CLI 命令
export DEFAULT_DELAY=1                   # tsc 消息发送延迟(秒)
export AGENT_LOG_DIR=~/.agent-logs       # 日志目录
```

## 项目结构

```
Tmux-AI-Team/
├── bashrc-ai-automation-v2.sh     # 核心 Bash 函数库 (2464 行)
├── CLAUDE.md                      # Claude Code 项目指南
├── .claude/
│   ├── TMUX_AI.md                 # Agent 运行时上下文
│   └── commands/tmuxAI/           # 斜杠命令 (13 个)
│       ├── start/pm-oversight     # PM 监督模式入口
│       ├── pm/                    # 槽位管理命令
│       └── roles/                 # 角色命令
├── hooks/                         # Claude Code Hook 配置
│   └── settings.template.json     # Stop/Prompt Hook 模板
└── docs/                          # 详细文档
    ├── 01-quick-start.md          # 快速开始
    ├── 03-pm-oversight-mode.md    # PM 监督模式手册
    └── 04-agent-roles.md          # Agent 角色
```

## 文档

- [快速开始](docs/01-quick-start.md) - 安装和基础用法
- [PM 监督模式](docs/03-pm-oversight-mode.md) - PM 槽位管理和 Hook 机制
- [Agent 角色](docs/04-agent-roles.md) - Developer、QA、DevOps、Reviewer
- [最佳实践](docs/05-best-practices.md) - 使用建议和常见问题

## 设计文档

- [项目隔离 PM 系统设计](项目隔离PM.md) - 详细的架构设计和原理说明

## License

MIT
